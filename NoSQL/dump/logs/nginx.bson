import pymongo

client = pymongo.MongoClient("mongodb://localhost:27017/")
db = client["your_database_name"]
collection = db["nginx"]

# Insert sample data
collection.insert_many([{"key": "value"} for _ in range(10)])
print("Inserted sample data.")
